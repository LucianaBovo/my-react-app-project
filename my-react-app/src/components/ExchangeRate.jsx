import React from 'react'

const ExchangeRate = () => {
  return (
    <div>
        <select>
            {/* here goes the dropdown for currency */}
        </select>
    </div>
  )
}

export default ExchangeRate